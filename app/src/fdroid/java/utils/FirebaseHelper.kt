package com.neilturner.aerialviews.utils

object FirebaseHelper {
    fun logException(ex: Throwable) {
        // No op
    }

    fun logScreenView(
        screenName: String,
        screenClass: Any,
    ) {
        // No op
    }
}
